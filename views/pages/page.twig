{% extends "layouts/base.twig" %}

{% block content %}
    <div class="post-wrap" id="post-{{post.ID}}">
        {{ post.title }}
        {{ post.content }}
    </div>
    {#{{dump(post)}}#}
{% endblock %}