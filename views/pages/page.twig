{% extends "layouts/base.twig" %}

{% block content %}

    {% do action('showBreads') %}

    <div class="post-wrap" id="post-{{ post.ID }}">

        <h1>{{ post.title }}</h1>

        {{ post.content }}

    </div>
{% endblock %}
