{% extends 'layouts/base.twig' %}

{% block content %}
    {% do action('woocommerce_before_cart') %}

    <div class='container'>
        <div data-block-name="woocommerce/cart" class="wp-block-woocommerce-cart alignwide">

            <div class='wc-block-components-notices'></div>
            <div class='wc-block-components-notices__snackbar wc-block-components-notice-snackbar-list'>
                <div></div>
            </div>

            <div class='row wc-block-cart wp-block-woocommerce-filled-cart-block is-large'>
                <div class='col-6 wc-block-components-main wc-block-cart__main wp-block-woocommerce-cart-items-block'>
                    <form class="woocommerce-cart-form" action="{{ site.url }}/cart/" method="post">
                        {% do action('woocommerce_before_cart_table') %}

                        <table class="table shop_table shop_table_responsive cart woocommerce-cart-form__contents">
                            <thead>
                            <tr>
                                <th class="product-thumbnail"><span
                                            class="screen-reader-text">Thumbnail image}</span></th>
                                <th class="product-name">Product</th>
                                <th class="product-price">Price</th>
                                <th class="product-quantity">Quantity</th>
                                <th class="product-subtotal">Subtotal</th>
                                <th class="product-remove"><span
                                            class="screen-reader-text">Remove item</span></th>
                            </tr>
                            </thead>
                            <tbody>
                            {% do action('woocommerce_before_cart_contents') %}

                            {% for cart_item_key, cart_item in cart.get_cart() %}
                                {% set _product = cart_item['data'] %}
                                {% set product_id = cart_item['product_id'] %}
                                {% set product_name = _product.get_name() %}

                                {% if _product and _product.exists() and cart_item['quantity'] > 0 and (cart_item|apply_filters('woocommerce_cart_item_visible', true, cart_item, cart_item_key)) %}

                                    {% set cart_item_class = 'cart-item' %}
                                    <tr class="woocommerce-cart-form__cart-item {{ cart_item_class }}">
                                        <td class="product-thumbnail">
                                            {% set thumbnail = _product.get_image() %}
                                            <a href="{{ function('get_permalink', product_id) }}">
                                                {{ thumbnail|raw }}
                                            </a>
                                        </td>

                                        <td class="product-name" data-title="Product">
                                            <a href="{{ function('get_permalink', product_id) }}">
                                                {{ product_name }}
                                            </a>

                                            {% do action('woocommerce_after_cart_item_name', cart_item, cart_item_key) %}

                                            {{ formatted_cart_item_data|raw }}

                                            {% if _product.backorders_require_notification() and _product.is_on_backorder(cart_item['quantity']) %}
                                                <p class="backorder_notification">
                                                    Available on backorder
                                                </p>
                                            {% endif %}
                                        </td>

                                        <td class="product-price" data-title="Price">
                                            {{ fn('apply_filters', 'woocommerce_cart_item_price', cart.get_product_price(_product), cart_item, cart_item_key) }}
                                        </td>

                                        <td class="product-quantity" data-title="Quantity">
                                            {% if _product.is_sold_individually() %}
                                                {% set min_quantity = 1 %}
                                                {% set max_quantity = 1 %}
                                            {% else %}
                                                {% set min_quantity = 0 %}
                                                {% set max_quantity = _product.get_max_purchase_quantity() %}
                                            {% endif %}

                                            {% set product_quantity %}
                                                <div class="wc-block-cart-item__quantity">
                                                    <div class="wc-block-components-quantity-selector">


                                                        <button aria-label="Reduce quantity of Hat" type="button"
                                                                class="wc-block-components-quantity-selector__button wc-block-components-quantity-selector__button--minus">
                                                            －
                                                        </button>

                                                        {{ function('woocommerce_quantity_input', {
                                                            'input_name': "cart[#{cart_item_key}][qty]",
                                                            'input_value': cart_item.quantity,
                                                            'max_value': max_quantity,
                                                            'min_value': min_quantity,
                                                            'product_name': product_name
                                                        }, _product, false) }}

                                                        <button aria-label="Increase quantity of Hat" type="button"
                                                                class="wc-block-components-quantity-selector__button wc-block-components-quantity-selector__button--plus">
                                                            ＋
                                                        </button>
                                                    </div>
                                                </div>
                                            {% endset %}

                                            {{ fn('apply_filters', 'woocommerce_cart_item_quantity', product_quantity, cart_item_key, cart_item) }}
                                        </td>

                                        <td class="product-subtotal" data-title="Subtotal">
                                            {% set sub_total = cart.get_product_subtotal(_product, cart_item.quantity) %}
                                            {{ fn('apply_filters', 'woocommerce_cart_item_subtotal', sub_total, cart_item, cart_item_key) }}
                                        </td>

                                        <td class="product-remove">
                                            <a href="{{ site.url }}/cart/?remove_item={{ cart_item_key }}"
                                               class="remove"
                                               aria-label="Remove from cart"
                                               data-product_id="{{ product_id }}"
                                               data-product_sku="{{ _product.get_sku() }}">&times;</a>
                                        </td>
                                    </tr>
                                {% endif %}
                            {% endfor %}

                            {% do action('woocommerce_cart_contents') %}

                            <tr>
                                <td colspan="6" class="actions">
                                    {% if coupons_enabled|default(false) %}
                                        <div class="coupon">
                                            <label for="coupon_code" class="screen-reader-text">Coupon</label>
                                            <input type="text" name="coupon_code" class="input-text" id="coupon_code"
                                                   value=""
                                                   placeholder="Coupon code" />
                                            <button type="submit"
                                                    class="button{{ ' ' ~ button_class_name|default('') }}"
                                                    name="apply_coupon" value="Apply coupon">
                                                Apply coupon
                                            </button>
                                            {% do action('woocommerce_cart_coupon') %}
                                        </div>
                                    {% endif %}

                                    <button type="submit" class="button{{ ' ' ~ button_class_name|default('') }}"
                                            name="update_cart" value="Update cart">
                                        Update cart
                                    </button>

                                    {% do action('woocommerce_cart_actions') %}

                                    <input type="hidden" name="woocommerce-cart-nonce"
                                           value="{{ woocommerce_cart_nonce }}" />
                                </td>
                            </tr>

                            {% do action('woocommerce_after_cart_contents') %}
                            </tbody>
                        </table>

                        {% do action('woocommerce_after_cart_table') %}
                    </form>

                    {% do action('woocommerce_before_cart_collaterals') %}
                </div>
                <div class='col-6 wc-block-components-sidebar wc-block-cart__sidebar wp-block-woocommerce-cart-totals-block'>
                    <div class="cart-collaterals">
                        {% do action('woocommerce_cart_collaterals') %}
                    </div>
                </div>
            </div>
        </div>
    </div>



    {% do action('woocommerce_after_cart') %}
{% endblock %}