{% extends 'layouts/base.twig' %}

{% block content %}
  <div class="post-wrap" id="post-{{ post.ID }}">
    {{ post.title }}

    {{ post.content }}

    {{ post._new_short_text }}
  </div>
{% endblock %}
